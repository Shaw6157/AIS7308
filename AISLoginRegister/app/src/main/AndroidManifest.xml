<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ais.aisloginregister">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ais_loginregister"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ais_loginregister_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AIS_LoginRegisterActivity">
            <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>
</activity>
        <activity android:name=".AIS_LoginStudentActivity" />
        <activity android:name=".AIS_RegisterStudentActivity" />
        <activity android:name=".AIS_HomeActivity" />
        <activity android:name=".AIS_ProgrammeActivity"></activity>
    </application>

</manifest>